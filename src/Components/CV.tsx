import styled from "styled-components"

const CVContainer = styled.div``

const CV = () => {
  return (
    <CVContainer>
      <h1>This is the CV page.</h1>
    </CVContainer>
  )
}

export default CV
